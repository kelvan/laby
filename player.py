class player:
	def __init__(self):
		self.points = 0
		self.finish = 0
	def raise_points(self):
		self.points += 1
	def set_points(self, points):
		self.points == points
	def set_finish(self, f):
		self.finish = f
